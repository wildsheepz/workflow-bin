# betterleaks

## Usage

``` yaml
    steps:
      - name: run action
        uses: wildsheepz/workflow-bin/setup-betterleaks@betterleaks

      - name: check executable
        shell: bash
        run: betterleaks --help
```
